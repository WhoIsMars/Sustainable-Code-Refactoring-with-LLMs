#if !defined(FOOD_CHAIN_H)
#define FOOD_CHAIN_H

#include <string>

namespace food_chain {

using namespace std;

string verse(int num);
string verses(int from, int to);
string sing();
}  // namespace food_chain

#endif  // FOOD_CHAIN_H